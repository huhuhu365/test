# 全部项目启动指南

更新日期：2026-09-10。面向 Windows PowerShell，依据当前代码与 package.json 核对。

## 1. 先选项目，再运行命令

`PS C:\wang\my-vue>` 表示当前终端在学生前端目录。输入命令时不用把 `PS ...>` 复制进去。
根目录 `C:\wang` 只统一管理 Git，没有应用启动脚本。

| 项目及详细说明 | 项目目录 | 前端/主程序 | 后端 | 地址或限制 |
| --- | --- | --- | --- | --- |
| [学生管理](./my-vue/README.md) | `my-vue` | `pnpm dev` | 学生后端源码未定位 | 前端请求 `http://localhost:8080/api/students` |
| [二手车管理](./used-car-management/README.md) | `used-car-management` | `pnpm dev` | 在其 `backend/` 中执行 `mvn spring-boot:run` | API 8080，`/api/vehicles` |
| [小满食堂](./mobile-order/README.md) | `mobile-order` | `pnpm dev` | 与前端一起启动 | 同源 `/api/menu`，需要本地 D1 运行环境 |
| [批量信件](./bulk-letter-vue/README.md) | `bulk-letter-vue` | `pnpm dev` | 另一个终端 `pnpm run server` | Express 3002，需要登录；发送会产生真实邮件 |
| [React 学习清单](./react-learning/README.md) | `react-learning` | `pnpm dev` | 另一个终端 `pnpm run server` | Express 3001，`/api/tasks` |
| [React 基础](./react-basic/README.md) | `react-basic` | `pnpm start` | 另一个终端 `pnpm run serve` | 前端通常 3000，JSON 模拟服务 3004 |
| [内部权限申请管理](./access-request-system/README.md) | `access-request-system` | `pnpm dev` | 另一个终端 `pnpm serve`（或 `pnpm start` 一起启动） | 前端 5173，json-server 3005，`/api` 已代理 |
| [ToDo](./todo-app/README.md) | `todo-app` | `pnpm dev` | 不需要 | 浏览器 localStorage 保存任务 |
| [MuleSoft](./mulesoft-hello-world/README.md) | `mulesoft-hello-world` | 没有前端 | Studio 中 Run As → Mule Application | 8081，`/hello`；Maven 打包不等于启动 |
| [Codex Pulse](./wang1/README.md) | `wang1` | 双击中文启动 BAT | 桌面版不需要 Web 后端 | 浏览器版用 `pnpm dev`，5180，开发 API 随 Vite 启动 |

除特别标注外，Vite 页面通常使用 5173；实际地址以终端为准。不要认为所有项目都是同一个端口或同一个后端。

## 2. 你当前的学生项目

```powershell
cd C:\wang\my-vue
pnpm dev
```

这只启动前端。当前源码需要学生 API，但 `C:\wang` 中没有该后端实现，因此不能在这里用一条命令启动它。
`students_seed.sql` 是数据库脚本，不是 API 服务。不要用二手车的后端替代。
学生后台需要另行定位或补建，接口契约及排查方式已写在 [学生 README](./my-vue/README.md)。

## 3. 二手车完整启动例子

终端 1：

```powershell
cd C:\wang\used-car-management\backend
$env:JAVA_HOME = 'C:\Program Files\Java\jdk-17.0.18'
mvn spring-boot:run
```

终端 2：

```powershell
cd C:\wang\used-car-management
pnpm dev
```

JDK 路径是当前电脑的实际路径；换电脑后应核对。后端成功启动后访问 `http://127.0.0.1:8080/api/vehicles`。

## 4. start、dev、server 的区别

它们是 package.json 中作者定义的脚本名，不能互换，也不会自动寻找另一个项目的后端。

| 用途 | pnpm | npm |
| --- | --- | --- |
| 列出当前项目可用脚本 | `pnpm run` | `npm run` |
| Vite 开发 | `pnpm dev` | `npm run dev` |
| Express 后端（仅有 server 脚本的项目） | `pnpm run server` | `npm run server` |
| React 基础页面 | `pnpm start` | `npm start` |
| 构建（仅有 build 脚本的项目） | `pnpm run build` | `npm run build` |

`pnpm start` 报 `ERR_PNPM_NO_SCRIPT_OR_SERVER` 时，先用 `pnpm run` 检查当前项目脚本，不要反复尝试相同命令。
`pnpm run build` 通常只生成构建产物，不会自动启动 API。

## 5. 依赖、端口与停止

- 已安装依赖时直接运行启动命令；首次安装使用项目已选定的包管理器和锁文件。不要为修复脚本名错误随意删除或重建锁文件。
- npm 与 pnpm 均能运行 package.json 脚本，但安装依赖时不要来回切换。当前学生项目已有 pnpm 锁文件，继续使用 pnpm 即可。
- 各项目的 Node 要求见自身 package.json；学生项目要求 `^22.18.0 || >=24.12.0`，点单项目要求 `>=22.13.0`。
- 一个终端运行服务后不会立即返回提示符是正常现象。启动另一个服务时开新终端。
- 用 `Ctrl + C` 停止当前终端服务。若提示端口被占用，先确认是不是已经启动过同一个服务。
- 学生前端预期端口 8080 的学生 API，与车辆后端的 8080 可能冲突；同端口上运行的车辆 API 不会自动获得学生接口。
- pnpm/npm 构建或预览成功不代表数据库、接口或线上部署均正常。

## 6. 本次文档核对范围

本次只更新 Markdown，不新增学生后端、不改变端口、不重新部署网站，也不执行邮件发送或数据库初始化。
已按源码核对脚本名、端口和前后端关系；尚未启动所有项目逐一做浏览器验收。
二手车的字段、HTTP 示例和已验证测试结果见 [详细要件与 API 说明](./used-car-management/要件详情与API使用说明.md)。

返回 [项目目录](./README.md)。
